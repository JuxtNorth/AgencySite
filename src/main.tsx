import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <main className="h-dvh w-screen grid place-items-center">
    	<h1 className="text-9xl text-slate-800 font-extrabold">Hello, World!</h1>
    </main>
  </React.StrictMode>,
)
